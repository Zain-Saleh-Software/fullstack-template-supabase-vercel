# Public Assets Directory

This directory contains static assets served by Next.js.

## POC Demo Assets (Remove After Bootstrap)

The following SVGs are POC demo assets that should be removed once the real project code is in place (see `skills/poc-cleanup.md`):

- `window.svg` — POC demo asset
- `vercel.svg` — POC demo asset
- `next.svg` — POC demo asset
- `globe.svg` — POC demo asset
- `file.svg` — POC demo asset

## Infrastructure Assets (Keep)

Any assets you add for your real project (logos, icons, images, favicons) should be placed in this directory. These are your project's assets and should be kept permanently.
